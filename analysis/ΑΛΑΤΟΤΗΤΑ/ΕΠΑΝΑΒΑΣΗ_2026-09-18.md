# ΕΠΑΝΑ-ΒΑΣΗ ΤΟΥ ΣΧΕΔΙΟΥ ΦΑΣΗΣ Α — 18/9/2026

Συμπληρώνει το `ΣΧΕΔΙΟ_ΕΚΤΕΛΕΣΗΣ_ΑΛΑΤΟΤΗΤΑ_ΦΑΣΗ_Α_2026-09-16.md` (§1.2 «Ζωντανή βάση»). Το σχέδιο
μένει ως έχει· εδώ γράφεται **τι μετρήθηκε στο ζωντανό TagoIO στις 18/9 ~12:00 UTC** και τι αλλάζει
στις γραμμές-στόχους. Κατάσταση: **ΤΙΠΟΤΑ ΔΕΝ ΕΚΤΕΛΕΣΤΗΚΕ** — εκκρεμούν τα 5 σημεία του §4.

## Απόφαση Μιχάλη 18/9

Ο κανόνας «Railway/GitHub SAG_CLEAN δεν αγγίζεται» του σχεδίου αφορούσε το repo της εφαρμογής SAG
v4.1 (PWA στο Railway), όχι το `maccHeraklion/sag-macc`. **Κάθε βήμα της Φάσης Α γίνεται commit +
push εδώ**, ώστε να το βλέπει ο Κώστας. Ο φάκελος `ΤΕΛΙΚΑ_2026-08-18` μένει αρχείο εργασίας και
έδρα του `ΜΗΤΡΩΟ_SAG.md`.

## Ζωντανή βάση — τι μετρήθηκε

| Συστατικό | Βάση σχεδίου (16/9) | Ζωντανό 18/9 | Κατάσταση |
|---|---|---|---|
| Πυρήνας `runPerTich` (analysis `6898958ea9fc0d000a382484`) | v50.134, sha256 `9946dc6e…` (CRLF, χωρίς BOM) | **Δεν κατεβαίνει** (MCP: «decompressed script exceeds 1 MiB»). Τελευταίο ανέβασμα script: **έκδοση 591, 11/9/2026 21:44:55 UTC**, από katharak@macc.gr. Καμία νεότερη έκδοση. Ρολόγια: lastHourTich 18/9 11:20 UTC, lastDailyTich 18/9 00:20 UTC | `ΥΠΟ ΕΛΕΓΧΟ` — δεν επαληθεύτηκε σε bytes, αλλά **δεν έχει ξανανέβει** μετά τη βάση. Το `analysis/runPerTich.js` του repo = v50.134: sha256 χωρίς BOM **= `9946dc6e…`** (`ΕΠΙΒΕΒΑΙΩΜΕΝΟ`). Το αντίγραφο του repo έχει UTF-8 BOM· το git το κρατά LF στο index (autocrlf) |
| Widget `storage/sagMain/index-7cbd9a4e.js` | md5 `23773b49…` (W-ECHIDE-01, 213.243 B) | **md5 `106a4dfaff0f5db3674c564b1dbda0d8`, 213.877 B, καθαρό LF, ανέβηκε 15/9/2026 17:08 UTC** (κατέβηκε από `api.tago.io/file/<profile>/storage/sagMain/…`) | `ΕΠΙΒΕΒΑΙΩΜΕΝΟ` — **η βάση του σχεδίου είναι παρωχημένη**. Το ζωντανό = το `_dist-sagMain/index-7cbd9a4e.js` του repo (ταυτόσημο μετά LF-κανονικοποίηση). Διαφορά από το `23773b49`: μόνο στο modal κανόνων άρδευσης — σήμα «✓ Στάλθηκε» ανά κανόνα + παύση 10 s μεταξύ αποστολών σε λειτουργία εύρους. Η λίστα `Ra` (…`"history_data","soil_ece1","soil_ece2"]`) υπάρχει **αμετάβλητη** → το βήμα 3 εφαρμόζεται πάνω στο repo bundle |
| `storage/sagMain/vendor-cc23ee3c.js` | — | 361.612 B, md5 `90d9ab2e…`, ανέβηκε 16/9 08:52 UTC = repo (LF) | `ΕΠΙΒΕΒΑΙΩΜΕΝΟ` μέγεθος· δεν αφορά το σχέδιο |
| Φόρμα `html_files/configuration.html` | γραμμές 515–517 / 2195 / 2507 / 2731 / 2905 (αντίγραφο 2.935 γρ. = v16) | **v18** (T-NEM3STATE-01), 188.270 B, md5 `75da935a…`, **2.950 γραμμές**, LF, χωρίς BOM, ανέβηκε 9/9 17:14:56 UTC | `ΕΠΙΒΕΒΑΙΩΜΕΝΟ` — ίδιο με το μητρώο. **Μπήκε στο repo** ως `custom_html_files/configuration.html` (το παλιό του repo ήταν έκδοση Ιουλίου, 1.106 γρ., χωρίς `water_ecw_dsm`) |
| `configurationoperational.html` | γραμμή 380 | **Δεν υπάρχει στο `html_files/`** του TagoIO (403 / απουσιάζει από τη λίστα 39 αρχείων) | Το βήμα 1 **δεν έχει δεύτερη φόρμα να συγχρονίσει**. Το αντίγραφο στο ΤΕΛΙΚΑ (21/8) είναι νεκρό |

## Νέες γραμμές-στόχοι του βήματος 1 (φόρμα v18, 2.950 γραμμές)

| Σχέδιο (βάση v16) | Ζωντανό v18 | Περιεχόμενο |
|---|---|---|
| 515 | **521** | `<label>Αλατότητα ECw (dS/m)</label>` |
| 516–517 | **522–523** | `<input id="water_ecw_dsm" …>` + βοήθεια |
| 2195 | **2208** | `water_ecw_dsm: numOrNull($('water_ecw_dsm').value),` |
| 2507 | **2522** | `if (conf.water_ecw_dsm!=null) $('water_ecw_dsm').value = …` |
| 2731 | **2746** | `['water_ecw_dsm','αγωγιμότητα νερού']]` |
| 2905 | **2920** | λευκή λίστα: `'irrig_fw_pct','irrig_efficiency_pct','water_ecw_dsm',` |

Οι γραμμές του πυρήνα (13623, 10857–10884, 10978–11020, 17495, 17551–17560, 18775) **δεν αλλάζουν** —
το repo είναι η v50.134.

## Τι ισχύει για την εκτέλεση

1. Βήμα 1 (φόρμα): βάση = `custom_html_files/configuration.html` (v18). Παραδίδεται στο `html_files/configuration.html`.
2. Βήμα 2, 4, 5, 6 (πυρήνας): βάση = `analysis/runPerTich.js`. Πριν από κάθε ανέβασμα ξανα-ελέγχεται ότι η
   analysis δεν απέκτησε νεότερη έκδοση από την 591 (`get_analysis` → versions).
3. Βήμα 3 (widget): βάση = `_dist-sagMain/index-7cbd9a4e.js` (md5 LF `106a4dfa…`). Πριν το ανέβασμα
   ξανα-κατεβαίνει το ζωντανό και συγκρίνεται.
4. Κάθε βήμα = commit στο `main` με subject `… - Michalis`, push αμέσως, και εγγραφή στο `ΜΗΤΡΩΟ_SAG.md`.
5. Εκκρεμούν (§4 του σχεδίου): ΟΚ βημάτων 1–6 ή υποσυνόλου · ΟΚ ζωνών R και «μέγιστο βαθών» · παράθυρο
   παγώματος με τον Κώστα · δήλωση `fert_ecw_dsm = 3,0` στον Γρινιαράκη μετά το βήμα 1 · πράξεις πεδίου Κώστα.

## Ενημέρωση 18/9 ~13:00 UTC — ΟΚ Μιχάλη για βήματα 1–3 · χτίστηκαν, ΔΕΝ ανέβηκαν

**Ζωντανός πυρήνας ΜΕΤΡΗΘΗΚΕ** (probe v3 στην analysis `6aa2d24f…`, διαδρομή `GET /analysis/{id}/download`,
gzip 342.672 → 1.256.750 B, 19.143 γρ., CRLF 19.142, 0 γυμνά LF, χωρίς BOM):
sha256 **`9946dc6e…` = v50.134**, `_SAG_SERIES_KEYS = ['soil_ece1', 'soil_ece2']`. Η βάση ισχύει.

| Παραδοτέο (commit `c86a521`) | Περιεχόμενο | Hash που πρέπει να δείξει το ζωντανό μετά το ανέβασμα |
|---|---|---|
| `analysis/runPerTich.js` **v50.135** | T-ECPORE-SERIES-01 (γρ. 13623 → 13624), BOM αφαιρέθηκε | sha256 CRLF `2460bc88b9541bf8b01cde5b62d2b243a851c99523c08fe835e292a574f20a32` · 1.256.921 B · 19.143 γρ. |
| `_dist-sagMain/index-7cbd9a4e.js` | λίστα `Ra` + `soil_ec_pore1/2` | md5 LF `080f28d0d369be4839e1714feca9d60d` · 213.909 B |
| `custom_html_files/configuration.html` **v19** | Φ1: 2 νερά + 2 ημερομηνίες, `strOrNull` | md5 LF `f5447b9813311b670a4d03979f08bca9` · 190.507 B · 2.972 γρ. |
| `analysis/setFieldParametersFromDeviceData.js` **v12** | ROOT_KEYS + 3 κλειδιά (αλλιώς η ετικέτα `configuration` τα κόβει — ο πυρήνας διαβάζει από την ετικέτα, γρ. 6089) | analysis `68fe925b3607df0010ef833c` |

Ελεγκτής `analysis/test/phaseA_salinity_check.mjs`: **22/22**, μεταλλάξεις **12/12 σκοτώθηκαν**.
Ο διαφορικός 64 αγρών ΔΕΝ έτρεξε (ο αρμοστής δεν υπάρχει σε αυτό το περιβάλλον)· απόδειξη λίτρων = 0 από
κώδικα: το `soil_ec_pore` εμφανίζεται στον πυρήνα μόνο στην εκπομπή (10971), στο regex μετακίνησης
(13245) και στη λίστα σειρών — κανένας υπολογισμός δεν το διαβάζει.

**Ανέβασμα — τι βρέθηκε:** το MCP κόβει download/upload στο 1 MiB και δεν ανεβάζει Files. Στην
προηγούμενη συνεδρία (15/9) λύθηκε ΜΟΝΟ το κατέβασμα (πολιτική `6aa2e0cf…`, `download_script`)· ανέβασμα
πυρήνα μέσω εργαλείου δεν είχε στηθεί ποτέ (τα ανεβάσματα τα έκανε ο Μιχάλης από το UI). Νέα διαδρομή:
`deploy/tago_deploy_probe.js` (GitHub raw σε καρφωμένο commit → sha256 → upload → επαλήθευση) — απαιτεί
πολιτική «[Analysis] SAG deploy από GitHub» (`upload_script` στον πυρήνα, `file/upload` στα
`storage/sagMain/`, `html_files/`), ενεργή μόνο κατά το deploy. Η δημιουργία της **μπλοκαρίστηκε από τον
ταξινομητή αδειών του Claude Code** — εκκρεμεί απόφαση Μιχάλη (άδεια ή δημιουργία από το UI).

**Σειρά ανεβάσματος (ανοιχτή):** 1) φόρμα v19 + sync v12 → αποθήκευση σε αγρό δοκιμής → ετικέτα με τα 3
κλειδιά · 2) πυρήνας v50.135 σε παράθυρο hh:25–hh:15 UTC → επόμενο tick: console χωρίς σφάλμα, νέες εγγραφές
`soil_ec_pore1/2` στη συσκευή αγρού Γρινιαράκη · 3) widget → δύο νέες γραμμές στο γράφημα.
Προαπαιτούμενο §4.3: παράθυρο παγώματος με τον Κώστα.
