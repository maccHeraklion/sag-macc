/**
 * Type declaration for the production Dashboard alias.
 * Resolved by Vite at build/dev time via @dashboard-src → ../src/Dashboard.tsx
 * Dashboard.tsx uses a named export (no default export).
 */
declare module "@dashboard-src/Dashboard" {
  import type React from "react";
  export const Dashboard: React.ComponentType;
}
